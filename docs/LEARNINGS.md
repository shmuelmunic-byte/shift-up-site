# LEARNINGS — Shift Up

---

# סשן: מגנט לידים "אבחון שיווק" — /audit (2026-07-20)

## מה בנינו
מהרעיון ("משהו בחינם לבעלי עסק") עד route חי בפרודקשן:
1. **`/audit`** (`src/pages/DiagnosticPage.jsx`) — שאלון 10 שאלות (שאלת מיקוד + 9 מדורגות ב-5 תחומים), ציון /100, פירוט לפי תחום, דליפות מדורגות, טופס ליד רך.
2. **פידבק ברמת התשובה** — כל תשובה חלשה (ציון 0/1) נושאת ניסוח ייעודי שמצטט את מה שנבחר ("אמרת ש...").
3. **חיבור לצינור הקיים** — `leads` ב-Supabase (`source:'diagnostic'`); ציון+דליפות+מייל נדחסו לתוך `message` (אין עמודת email/score → אפס מיגרציה).
4. **2 נקודות כניסה** — `AuditBanner.jsx` בדף הבית (אחרי סקשן הכאב) + כרטיס ב-`/freebies`.
5. **אנליטיקה אנונימית** — טבלת `audit_results` (`docs/audit-results-table.sql`) + טאב "🎯 אבחון" ב-/admin.

## טכניקות Claude Code שהשתמשנו בהן
- **אימות מול הקוד החי, לא צילום מסך.** הרצת `finish()` / `insert` דרך `javascript_tool` בדפדפן החזירה JSON מדויק (`INSERT_OK`, מבנה הדליפות, מקרי קצה) — תפס מה שעיניים מפספסות.
- **בדיקת מקרי קצה של ספירה** — 0/1/2/3+ דליפות נבדקו אחרי כל שינוי לוגיקה.
- **CSS ממופרד ב-prefix (`dg-`)** — מנע התנגשות עם `index.css` הגלובלי בלי CSS Modules.
- **שימוש חוזר בתשתית קיימת** — `leads` + Resend + `/admin` + Pixel/GA4 גלובליים במקום Formspree נפרד.
- **סקילים:** `ui-ux-pro-max` (נגישות מקלדת, `prefers-reduced-motion`, `for`/`id`), `shift-up-content` (הזמנה לקהילה).

## מה עבד טוב
- מיפוי לסכמה קיימת בלי מיגרציה — זרם ישר ל-`/admin` ולהתראת המייל.
- פידבק ברמת התשובה — הקפיץ את האבחון מ"גנרי" ל"אישי".
- דלת רכה שלא מתחרה ב-CTA הראשי.

## מה הייתי עושה אחרת
- **פידבק ברמת התשובה מההתחלה** — בניתי קודם פר-קטגוריה גנרי, המשתמש דרש דיוק. סבב מיותר.
- **מקרי קצה מוקדם יותר** — הבאג "3 דליפות גם בציון 100" נתפס רק כשהמשתמש הצביע.
- **⚠️ `Write` על קובץ קיים = דריסה.** דרסתי את `LEARNINGS.md` הזה (190 שורות מסשנים קודמים) במקום לקרוא ולהוסיף בראש. **תמיד `Read` לפני `Write` על קובץ שעשוי להתקיים** — או `Edit` להוספה. שוחזר מ-git.

## לקחים תוכניים (לא רק טכניים)
- **מתנה חינמית צריכה להדגים את הבידול, לא לסתור אותו.** אסטרטג מבוסס-מחקר → אבחון אינטראקטיבי, לא PDF גנרי.
- **מיינדסט נתינה בפועל:** הערך ניתן *לפני* הטופס ולא נעול מאחוריו; הטופס רך ואופציונלי.
- **AI-look בקופי:** מקף ארוך (—) הוא טל-טייל. בכל נכס פונה-חוץ להחליף ל-`-` קצר.
- **kill-words** — לעבור על כל נכס מול רשימת ה"הימנע" של המותג לפני שהוא יוצא.

## Skills ששווה לשמור
- **"מגנט לידים אינטראקטיבי — מהרעיון לפרודקשן"** — נלכד ב-vault: `Knowledge/מגנט לידים אינטראקטיבי.md`.

---

# סשן: הפיכת LEARNINGS לסקילים קבועים (2026-07-08)

## מה בנינו
קראנו את `docs/LEARNINGS.md` וחילצנו ממנו את כל הסקילים המומלצים. 4 כבר היו מותקנים (`ai-discovery-setup`, `hebrew-seo-geo-toolkit`, `sitemap-optimizer`, `landing-page-research`) — נמנעה יצירה כפולה. 3 היו חסרים ונבנו כסקילים גלובליים (`~/.claude/skills/`):
1. **`supabase-cms-fallback-first`** — הדפוס "fallback-first CMS על Supabase". SKILL.md + 4 נכסים דרופ-אין: `useContent.js` (האמיתי מהפרויקט), `GenericCrudTab.jsx` (מוכלל), `tab-example.jsx`, `setup-template.sql` (idempotent+RLS). כולל את כל ה-gotchas (jsonb→`[object Object]`, סוד ל-Vault, בונים עריך מלכתחילה).
2. **`landing-section-builder`** — בניית סקשן לפי spec נעול + זהות עיצוב + אימות preview. 60-30-10, gotchas של Lenis/lazy-load/auth, כלל "קובץ אחד = סוכן אחד".
3. **`hebrew-rtl-pdf-windows`** — `markdown`→HTML→Chrome headless. `build_pdf.py` הוכלל ל-argv (לא נעול לנתיב).

## טכניקות / לקחים
- **"מה כבר קיים?" לפני "תיצור".** בדיקת תיקיית הסקילים לפני יצירה חסכה 4 סקילים כפולים. תמיד לבדוק מלאי קודם.
- **נכסים = קבצים אמיתיים, לא הוראות.** חילצתי את `useContent.js`/`GenericCrudTab.jsx` מהקוד החי → הסקיל מדויק ולא ממציא. אותו עיקרון כמו ב-`shift-up-proposal-builder` (מצרפים build_pdf.py כקובץ).
- **הכללה קלה בזמן החילוץ.** `build_pdf.py` הנעול-לנתיב הפך ל-argv; `GenericCrudTab` קיבל סטיילים ניטרליים — כך הסקיל עובד בפרויקט הבא, לא רק כאן.
- **`description` = מנוע ההפעלה.** לכל סקיל כתבתי טריגרים מפורשים + "Do NOT use for..." שמפנה לסקיל המתחרה הנכון — אחרת הסקיל לא נטען.

---

# סשן: יישור כל הדפים + רענון AI-Discovery/SEO (2026-07-07 · מאוחר)

## מה בנינו
יישרנו את כל דפי האתר לדף הבית החדש, ואז רעננו את שכבת ה-AI-discovery וה-SEO.
1. **`/en` (`EnglishPage.jsx`) — בנייה מחדש 1:1** למשפך: Hero חדש ("Get back to running your business. Leave the marketing to me"), 5 סקשנים אנגליים חדשים (TrustStrip, Pain, WhatYouGet, Proof+lightbox, LeadForm רקע בהיר + insert ל-`leads` + נפילה לוואטסאפ). נמחקו Marquee+Stats; מניפסט הוזז אחרי Pain; הזמנת הקבוצה הועברה מתחת לטופס.
2. **`/ig` + `/freebies` + `/testimonials`** — יישור מסרים למיצוב הנעול, בלי המצאת תוצאות.
3. **AI Discovery** — `llms.txt` + `index.md` + `en.md` נכתבו מחדש למיצוב החדש (מחיר/scarcity/קריאייטיב/שמות שלבים). `faq.md` נשאר.
4. **SEO + GEO** — `index.html`: title/description/OG/Twitter, `hreflang he→he-IL`, Schema מורחב (slogan, `makesOffer` ILS, שעות א׳–ה׳ 08:00–17:00, `speakable`, `knowsAbout`). `robots.txt`: allow מפורש לבוטי AI. `sitemap.xml`: lastmod. **h1 fallback סטטי ב-`#root`** לסורקים ללא JS.
5. תיאור ל-Google Business Profile (≤750 תווים, בלי קישורים/מבצעים).

## טכניקות Claude Code שהשתמשנו בהן
- **סוכנים מקבילים על קבצים דיסיוינטיים** — 3 בו-זמנית (English כבד = סוכן ייעודי אחד). כלל: קובץ גדול אחד = סוכן אחד; לא לחלק קובץ בין סוכנים (התנגשות merge).
- **צמד סקילים ל-SEO/AI:** `ai-discovery-setup` + `hebrew-seo-geo-toolkit` — הביאו רשימת בוטי-AI עדכנית ועקרונות GEO, עדיף על ידע כללי.
- **אימות ב-preview** אחרי כל שינוי נצפה (screenshot + בדיקת DOM: מיקום סקשנים, ספירת h1, שה-fallback הוחלף נקי).
- **הרצת `seo_audit.py` של הסקיל** לאודיט חי — תפס את ה-`h1: MISSING`.

## מה עבד טוב
- חילוץ תוכן מהקומפוננטות החיות (לא המצאה) — הקבצים נשארו מדויקים למיצוב.
- אימות ה-h1 fallback משני הכיוונים: HTML גולמי (יש h1) + אחרי React (h1 יחיד, בלי כפילות/cloaking).

## מה פחות / לשים לב
- **PowerShell 5.1 שובר here-strings** מרובי-שורות → הודעות commit ארוכות דרך `git commit -F` מקובץ.
- **יוניקוד עברי ב-Python על Windows** קורס ב-cp1252 → תמיד `PYTHONUTF8=1`.
- `node -e` עם מרכאות נשבר ב-PowerShell → דרך Bash tool או קובץ.

## Gotcha מרכזי — SEO על SPA
דף React שולח HTML ראשוני **בלי h1/תוכן**; סורקים ללא JS לא רואים על מה הדף. פתרון זול: בלוק fallback מוסתר ב-`#root` עם h1+מיצוב ש-React מחליף ב-mount. פתרון מלא: pre-render/SSR.

## Skills ששווה לשמור
- `ai-discovery-setup` + `hebrew-seo-geo-toolkit` — צמד קבוע לכל אתר תדמית/נחיתה חדש.

---

# סשן: השלמת המשפך + CMS מלא + לולאת לידים (2026-07-07)

## מה בנינו
מימוש מלא של האפיון — דף הבית הפך לדף-נחיתה-שממיר עם CMS מלא, מקצה לקצה:
1. **השלמת המשפך (11 סקשנים):** `WhatYouGet` (6), `Proof` (8 — קייסים + גריד 9 מודעות + lightbox), `LeadForm` (11 — רקע בהיר, insert ל-`leads` + fallback לוואטסאפ). המניפסט הוזז למקום הנכון (4).
2. **תיקון גריד 2×2:** `auto-fit` ייצר 3+1 לא מאוזן בדסקטופ → class משותף `.grid-2x2` (Pain + WhatYouGet).
3. **לולאת לידים:** טבלת `leads` + RLS, טאב "📥 לידים" ב-/admin (טופל/מחיקה/וואטסאפ + badge), התראת מייל (טריגר `pg_net`→Resend, מפתח ב-Vault). הכל ב-`docs/setup.sql` — **הורץ ונבדק, המייל עובד**.
4. **CMS מלא:** 5 טבלאות חדשות (`trust_pillars`/`pain_points`/`outcomes`/`proof_cases`/`proof_grid`) + כל הכותרות/טקסטים ל-`site_content` דרך הוק `useContent()`. 5 טאבי admin חדשים דרך `GenericCrudTab`.
5. **שדרוג UI/UX אדמין:** רקע bedrock+aurora+noise, טאבים נגללים, focus glow ירוק, hover.

## טכניקות Claude Code שעבדו
- **Fallback-first כעיקרון על:** כל קומפוננטה מציגה `FALLBACK_*` מהקוד ומחליפה בשקט ב-DB. האתר לא נשבר גם כשה-SQL עוד לא רץ — ה-deploy מנותק מהרצת ה-DB.
- **preview MCP לאימות אמיתי:** `preview_eval` לקריאת ה-DOM (רשימת כותרות → הוכחת סדר), `preview_console_logs`, screenshot להוכחה. עדיף על "אני מניח שעובד".
- **שימוש חוזר ב-`GenericCrudTab`:** טאב CRUD חדש = ~12 שורות. הרחבת CMS ל-5 סקשנים בלי לוגיקה חדשה.
- **הפשטה נקודתית (`useContent`):** הוק אחד fallback-first במקום useEffect חוזר ב-5 קומפוננטות.
- **`.admin-scope` ל-CSS מעל inline styles:** `:focus`/`:hover` עם `!important` תחת wrapper — שדרג את כל השדות בלי לגעת ב-N קומפוננטות.
- **`AskUserQuestion` לסקופ גדול:** לפני 5 טבלאות שאלתי עומק CMS + מה עם סקשני האנימציה. חסך בנייה מיותרת.
- **SQL idempotent:** `if not exists` + seed `where not exists` + `on conflict do nothing` → בטוח להריץ שוב.

## מה פחות עבד / מלכודות
- **Lenis smooth-scroll הפריע לצילומי מסך** — פתרון: לולאת `scrollTo` שמתכנסת ליעד לפני הצילום.
- **האדמין נעול מאחורי auth** — עם Supabase placeholder מקומי אי אפשר לצלם מסך מחובר; הסתמכתי על build + redirect ל-/login.
- **lazy-load = layout shift** במדידות מיקום. `loading='eager'` זמני ייצב.
- **jsonb לעריכה ב-admin** הציג `[object Object]` → שיניתי `proof_cases.steps` ל-`text` שמכיל JSON.

## מה הייתי עושה אחרת
- **להתחיל מ-site_content, לא לרפקטר אליו** — בניתי hardcoded ואז הפכתי לעריך = סבב כפול. בסקשן עתידי: כותרות ב-`site_content` + כרטיסים בטבלה מלכתחילה, עם fallback.
- **סוד (Resend key) לעולם לא לקובץ ב-repo ציבורי** — הופנה ל-Supabase Vault דרך הרצה נפרדת.

## Skills ששווה לזכור
- הדפוס **"fallback-first CMS על Supabase + GenericCrudTab + useContent"** חוזר בין פרויקטים — מועמד ל-skill.

## נותר לך (צד משתמש)
1. לסנכרן/לחדד נוסחים ב-/admin (טאב "📝 תוכן + קשר" + טאבי הסקשנים).
2. להחליף מודעת SHIRA בגריד בגרסת קינוחי השבת.
3. רוטציית מפתח Resend (נחשף בצ'אט) — צור חדש, הרץ שוב את קטע ה-Vault, מחק ישן.

---

# סשן: בניית משפך דף הבית — סקשנים 1–4 (2026-06-24)

## מה בנינו
הפיכת דף הבית מאתר-תדמית לדף-נחיתה-שממיר לפי האפיון הנעול. 4/10 סקשנים + deploy ל-main:
1. **Hero** (`Hero.jsx`) — נבנה מחדש: כותרת נעולה, 2CTA (טופס ראשי / וואטסאפ משני), עוגן מחיר 1,500₪, scarcity, trust chips, ציאן ב-aurora.
2. **רצועת אמון** (`TrustStrip.jsx`, חדש) — סלקטיביות + זמינות חיה (08:00–17:00) + שיחת היכרות חינם. החליף את Marquee+Stats.
3. **כאב** (`PainSection.jsx`, חדש) — הוק "נמאס לרדוף..." + 4 כרטיסים + מעבר הקלה ירוק.
4. **3 שלבים** (`Process.jsx`) — שמות מותאמים ל-spec: פיצוח / שיפט / אקשן, הבלטת "קריאייטיב שמוכר".

## טכניקות Claude Code שהשתמשנו בהן
- **preview_* tools לאימות ויזואלי** — screenshot + resize (desktop/mobile) + eval לגלילה. בלי לבקש מהמשתמש לבדוק ידנית.
- **שאילתת Supabase REST ישירה** (curl + anon key) כדי לראות מה ה-DB מחזיק בפועל לפני החלטה.
- **קריאת ה-spec מהדיסק לפני כל סקשן** — עיגון התוכן באפיון נעול במקום המצאה.
- שימור מנוע האנימציה הקיים (GSAP/kinetic) והחלפת תוכן בלבד — מהיר ובטוח.

## מה עבד טוב
- בנייה סקשן-סקשן עם אימות ויזואלי בכל שלב.
- **משמעת צבע 60-30-10** (ירוק רק על פעולה/הקלה) → ניגוד רגשי חזק בסקשן הכאב.

## מה פחות עבד / לקח מרכזי
- **דפוס CMS-override:** תוכן "נעול" שיושב ב-Supabase דורס את ה-fallback בקוד (subtitle + שלבים). ה-.env המקומי read-only → סנכרון = פעולת משתמש ב-/admin. **מסקנה:** לפני כל סקשן CMS — לשלוף את ערכי ה-DB, ולרכז את כל הסנכרונים לרשימה אחת בסוף.

## Skills ששווה
- רעיון: skill "section-builder" — מקבל בריף-סקשן + spec + זהות עיצוב, בונה קומפוננטה ומאמת ב-preview.

## נותר לך
- לסנכרן DB ב-/admin (subtitle + שלבי תהליך). המשך: סקשנים 5–10. מצב מלא ב-CLAUDE.md.

---

# סשן: מחקר + אפיון שדרוג דף הבית (2026-06-24)

## מה בנינו
תשתית **research → spec → context** לשדרוג דף הבית מאתר-תדמית לדף-נחיתה-שממיר, בשיטת הסקיל `landing-page-research`:
- **הסקיל** הותקן ב-`.claude/skills/landing-page-research/SKILL.md` (ריאיון → תוכנית → מחקר עם מקורות → HTML).
- **`docs/landing-research.html`** — מחקר מלא: מיפוי 3 מתחרים סקשן-אחרי-סקשן, פער תחרותי, מבנה משפך מומלץ (11 סקשנים), כיוון עיצובי. מקור לכל טענה.
- **`docs/landing-spec.html`** — אפיון דף אחד: מטרה, קהל, התנגדויות, מסר, טון, פלטה, מבנה, בריף תוכן, SEO, מדדים.
- **`docs/portfolio-manifest.md`** + `public/portfolio/` — 9 מודעות נבחרות (9 תחומים) + 2 קייסים אסטרטגיים (קינוחי בוטיק אנונימי + City Transformer).
- **CLAUDE.md** — סעיף "פרויקט שדרוג דף הבית" עם כל ההחלטות הנעולות.

## החלטות מפתח שננעלו
- Hero: **"תחזור לנהל את העסק. את השיווק תשאיר לי."**
- מיצוב: אדם אחד (לא סוכנות) + מסר לפני תקציב + מהירות AI. לא ממצבים על דיוור.
- הוכחה בלי עדויות: קייסים אסטרטגיים + גריד קריאייטיב + שיחת פיצוח חינם + סלקטיביות.
- עוגן מחיר "מתחיל מ-1,500 ₪", scarcity "2–3 מקומות", ליד ל-Supabase, טופס על רקע בהיר.

## טכניקות Claude Code שהשתמשנו בהן
- **סקיל שמראיין לפני שחוקר** — הכוח של `landing-page-research`: שאלה-אחר-שאלה מוציא ממך מה שרק אתה יודע, לפני שהמחקר מנחש.
- **AskUserQuestion לצמתים אמיתיים** — כל החלטה מהותית (קהל, מטרה, מסר, מיצוב) עברה דרך שאלה ממוקדת, לא ניחוש.
- **מבט אאוטסיידר עם מקורות** — WebSearch + WebFetch למיפוי מתחרים בפועל (פתחתי דפים, מיפיתי סקשנים), לא מהזיכרון.
- **חילוץ טקסט מ-PDF ענק** — ה-Read נכשל על PDF-ים (אין pdftoppm); עקפתי עם `pypdf` → קובץ UTF-8 → Read. ככה קראתי את מצגות City Transformer + שירה.
- **HTML → PDF בעברית RTL** — WeasyPrint לא עובד ב-Windows (צריך GTK); הפתרון: `markdown`→HTML מעוצב→**Chrome headless `--print-to-pdf`**. RTL מושלם.
- **עדכון-תוך-כדי** — כל תיקון שלך (ציאן, מניפסט, קבוצה שקטה, אנונימיזציה) נכנס מיד לאפיון+מחקר+קונטקסט, ואודיט סופי לניקוי שאריות.

## מה עבד טוב
- **פורמט HTML למסמכי עבודה חיים** — קל לעדכן תוך כדי דיאלוג, נעים לקריאה, נפתח ב-preview.
- **הפיכת חולשה ליתרון** — "אין עדויות" → מכירת שיטה+אדם+קריאייטיב; פיבוט שירה שנפל → קייס אנונימי חזק יותר.
- **מקור אמת אחד (CLAUDE.md)** — כל ההחלטות מרוכזות, כך שמפגש הבנייה יתחיל מלא-הקשר.

## מה פחות עבד / לשים לב
- **תאריכים** — כדאי לנעול תאריך פרויקט אחד ולהיצמד אליו (הייתה אי-בהירות מול תאריך המערכת).
- **קבצי מדיה מהצ'אט** — תמונות שמודבקות בצ'אט לא ניתנות לשמירה כקבצים; המשתמש חייב לגרור אותן ידנית לתיקייה.

## מה הייתי עושה אחרת
- לבקש את נכסי הקריאייטיב **בתחילת** הריאיון — הם שינו את אסטרטגיית ההוכחה (קייסים דו-שכבתיים), וטוב היה לדעת מוקדם.

## Skills ששווה לשמור
- **`landing-page-research`** — כבר מותקן. תהליך חוזר לכל דף נחיתה עתידי (גם ללקוחות).
- **"HTML→PDF עברי RTL ב-Windows"** — דפוס Chrome-headless ששווה להפוך לסקריפט קבוע (`docs/_build_pdf.py` הוא הבסיס).

## נותר לך (לפני מפגש הבנייה)
1. להחליף מודעת SHIRA בגריד בגרסת קינוחי השבת המעודכנת.
2. לוודא ש-`retail-kehilot-card.jpg` = מודעת ה-5%.
3. לייצא GA4 + GSC (90 יום) לכיול יעד ההמרה.
4. לפתוח שיחה חדשה ולומר "בוא נבנה את ה-Hero".

---

# סשן AI Discovery (2026-06-19)

## מה בנינו
שכבת **AI Discovery** לאתר Shift Up — לגרום ל-LLMs (ChatGPT/Claude/Perplexity) למצוא, להבין ולהמליץ על העסק:
- `public/llms.txt` — מדריך AI ראשי (פורמט llmstxt.org), דו-לשוני עברית+אנגלית.
- 3 **Markdown Mirrors** — גרסאות markdown נקיות של הדפים: `index.md`, `faq.md`, `en.md`.
- `robots.txt` — נוספה שורת `LLM:` שמכוונת קראולרים ל-llms.txt.
- `sitemap.xml` — שוכתב: נוספו `/testimonials` + `/freebies`, עודכן lastmod, הוסר changefreq (Google מתעלם ממנו מ-2023).
- תיעוד מלא ב-CLAUDE.md + לוג שינויים.
- הבהרה ב-CLAUDE.md: דוח GSC "Page with redirect" הוא צפוי, לא תקלה.

## טכניקות Claude Code שהשתמשנו בהן
- **חילוץ תוכן אמיתי מהקוד** — את ה-markdown mirrors בניתי מהטקסט האמיתי בקומפוננטות (Hero/Process/WhyMe/FAQ/About), לא המצאתי. ככה התוכן מדויק.
- **אימות הנחה לפני פעולה** — לפני יצירת קבצי .txt/.md בדקתי ש-`vercel.json` (rewrite עם החרגת נקודה) באמת יגיש אותם סטטית ולא יחטוף ל-index.html.
- **הרשאת deploy מפורשת** — ה-classifier חסם merge ל-main כי הוא מפעיל deploy לפרודקשן; נדרשה הסכמה מפורשת שלך לפני המיזוג.
- **/compact** באמצע הסשן לפינוי הקשר.

## מה עבד טוב
- Fallback-first בכל הקומפוננטות — האתר עמיד גם אם Supabase נופל.
- תיעוד מיידי אחרי כל שינוי משמעותי ב-CLAUDE.md → הידע לא הולך לאיבוד בין סשנים.

## מה פחות עבד / לשים לב בפעם הבאה
- **`gh` לא מותקן** במחשב — נאלצנו לעקוף עם git רגיל במקום GitHub PR flow. שווה להתקין: `winget install GitHub.cli`.
- **הודעות commit עם מירכאות כפולות** שברו את ה-parsing ב-PowerShell. הפתרון: `-m` עם גרש בודד (`'...'`), או `-m` נפרד לכל שורה.

## מה הייתי עושה אחרת
- מתקין `gh` מראש כדי לעבוד עם PRs נקיים (review, היסטוריה).
- מגדיר Domain property ב-GSC מההתחלה — מונע את דוחות ה-redirect המבלבלים.

## Skills ששווה לשמור
- **"AI Discovery setup"** — skill שמייצר llms.txt + markdown mirrors מתוך קומפוננטות אתר. תהליך חוזר שיעבוד על כל אתר עתידי. שווה להכין פעם אחת.
- **"Sitemap optimizer"** — בדיקה+תיקון של sitemap.xml לפי best practices עדכניים (lastmod, hreflang, בלי changefreq).

## צעדים שנותרו לך (צד משתמש)
1. להגיש מחדש את sitemap.xml ב-Google Search Console.
2. בעוד כמה ימים — לבדוק LLMs: לשאול ChatGPT/Claude "מי זה שמואל מוניץ Shift Up?".
3. (אופציונלי) להחליף ל-Domain property ב-GSC.
