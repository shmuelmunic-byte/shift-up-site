-- ════════════════════════════════════════════════════════════════════════
-- Shift Up · סנכרון "מיינדסט שפע" (2026-07-20) ל-Supabase
-- ────────────────────────────────────────────────────────────────────────
-- מיינדסט: כותבים מזווית נתינה — מה משתנה לעסק הלקוח — לא flex על המוכר.
-- מקור: הוולט → Knowledge/מיינדסט שפע — עבודה כנתינה.md
--
-- ⚠️ טיוטה לאישור שמואל לפני הרצה. משנה טקסט חי באתר.
-- מריצים פעם אחת: Supabase → SQL Editor → New query → הדבק → Run.
-- idempotent (upsert / update ממוקד).
-- ⚠️ דורס עריכה ידנית ב-/admin רק בשורות שמופיעות כאן.
--
-- הערה: רוב האתר כבר כתוב מזווית נתינה (הבטחת ביצוע, שותף ביצוע, מחקר
-- ששומר על התקציב שלך). זה מתקן רק נקודות שממוסגרות מזווית "לקיחה".
-- ════════════════════════════════════════════════════════════════════════

-- ── 1. why_me "AI ראשון" — flex על המוכר → תועלת ללקוח ────────────────────
-- לפני: "מה שלוקח לאחרים שבוע — אני מבצע בכמה שעות, ובאיכות גבוהה יותר."
update public.why_me_reasons
set description = 'מה שלוקח לאחרים שבוע קורה אצלך בשעות — יותר גרסאות נבדקות, פחות תקציב מתבזבז, והעסק שלך באוויר מהר יותר.'
where title = 'AI ראשון';

-- ── 2. hero.subtitle: להוסיף את התוצאה ללקוח (אושר 2026-07-20) ─────────────
-- לפני: "קמפיינים מבוססי-מחקר לעסקים שכבר מבינים שיווק — עם הבטחת ביצוע על חודש הניהול הראשון."
update public.site_content
set value = 'קמפיינים מבוססי-מחקר שמחזירים לעסק פניות ושקט — לעסקים שכבר מבינים שיווק, עם הבטחת ביצוע על חודש הניהול הראשון.'
where key = 'hero.subtitle';
